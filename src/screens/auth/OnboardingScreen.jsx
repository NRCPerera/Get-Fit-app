import React from 'react';
import OnboardingNavigator from './OnboardingNavigator';

const OnboardingScreen = ({ navigation }) => {
  return <OnboardingNavigator navigation={navigation} />;
};

export default OnboardingScreen;
